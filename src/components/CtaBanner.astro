<section
  id="planes"
  class="relative w-full overflow-visible bg-[#6255FA] opacity-0 transition-all duration-700"
>
  <!-- Fondo diagonal -->
  <img
    src="/svg/diagonal_background.svg"
    alt="Fondo diagonal"
    class="absolute inset-0 w-full h-full object-cover z-0"
  />

  <!-- Contenido -->
  <div
    class="relative z-10 max-w-7xl mx-auto px-4 pt-16 pb-0 md:py-0 md:h-[456px] flex flex-col md:flex-row items-center md:items-center justify-between gap-6"
  >
    <!-- Texto -->
    <div class="max-w-lg text-white text-center md:text-left">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">
        ¿Listo para empezar a aprender con nosotros?
      </h2>
      <p class="text-sm md:text-base mb-6">
        Descubre todos los planes disponibles y elige el que mejor se adapte a ti.
      </p>
      <a
        href="#planes-precios"
        class="bg-white text-black font-semibold py-2 px-6 rounded-full shadow hover:bg-gray-100 transition"
      >
        Ver planes y precios
      </a>
    </div>

    <!-- Imagen estudiante -->
    <!-- Móvil: visible y con altura ajustada -->
    <div class="block md:hidden -mb-0">
      <img
        src="/students/estudiante.png"
        alt="Estudiante"
        class="h-[420px] object-contain mx-auto"
      />
    </div>

    <!-- Escritorio: imagen sobresale -->
    <div class="hidden md:block absolute -top-14 right-10 z-10">
      <img
        src="/students/estudiante.png"
        alt="Estudiante"
        class="h-[512px] object-contain"
      />
    </div>
  </div>
</section>

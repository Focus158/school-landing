<main
  id="inicio"
  class="scroll-mt-[40px] flex flex-col lg:grid lg:grid-cols-2 place-items-center lg:place-items-center pt-4 pb-2 sm:pt-6 sm:pb-4 lg:pt-10 lg:pb-10 overflow-hidden relative bg-gradient-to-br from-[#B9A7FF] to-[#6255FA] min-h-screen"
>
  <!-- DECORACIONES ANIMADAS -->
  <div class="absolute inset-0 z-0 pointer-events-none overflow-hidden">
    <div class="absolute top-0 left-1/2 w-[4px] h-[300%] bg-[#6255FA] rounded-full animate-wave-line opacity-30"></div>
    <div class="absolute top-[5%] left-[8%] w-6 h-6 bg-[#6255FA] rounded-full opacity-30 blur-sm animate-float1"></div>
    <div class="absolute bottom-[20%] right-[10%] w-4 h-4 bg-[#6255FA] clip-triangle opacity-30 animate-float2"></div>
    <div class="absolute top-[50%] left-[65%] w-5 h-5 bg-[#6255FA] rounded-sm opacity-30 animate-float3"></div>
    <div class="absolute top-[25%] right-[40%] w-3 h-3 bg-[#6255FA] rounded-full opacity-40 animate-float4"></div>
    <div class="absolute top-[15%] left-[30%] w-3 h-3 bg-[#6255FA] rotate-45 opacity-20 animate-float5 lg:hidden"></div>
    <div class="absolute bottom-[30%] left-[15%] w-4 h-4 bg-[#6255FA] rounded-full opacity-25 animate-float6 lg:hidden"></div>
    <div class="absolute bottom-[10%] right-[35%] w-5 h-5 bg-[#6255FA] rotate-[30deg] rounded-sm opacity-20 animate-float7 lg:hidden"></div>
    <div class="absolute top-[38%] right-[15%] w-3 h-3 bg-[#6255FA] clip-diamond opacity-30 animate-float8 lg:hidden"></div>
    <div class="absolute top-[20%] left-1/2 transform -translate-x-1/2 w-[260px] h-[260px] bg-white/10 rounded-full blur-2xl z-0 lg:hidden"></div>
  </div>

  <!-- Decoración esquina -->
  <img src="/svg/deco_corner.svg" alt="Decoración inferior" class="absolute bottom-4 right-4 w-12 h-12 z-10 lg:hidden opacity-60" />

  <!-- Ilustración Lottie móvil -->
<div class="flex lg:hidden justify-center px-4 order-1 relative -mt-9 sm:-mt-11 items-start">
    <script src="https://unpkg.com/@dotlottie/player-component@2.7.12/dist/dotlottie-player.mjs" type="module"></script>
    <dotlottie-player
      src="https://lottie.host/463208d5-6946-44c6-aa6f-d368fc70683d/guEAFpqxHs.lottie"
      background="transparent"
      speed="1"
      loop
      autoplay
      class="w-[320px] h-[320px] sm:w-[300px] sm:h-[300px] animate-fade-in"
    ></dotlottie-player>
  </div>

  <!-- TEXTO -->
  <div class="z-20 max-w-xl px-6 text-white order-2 lg:order-1 relative">
    <!-- Título móvil -->
    <h1 class="text-5xl leading-tight font-bold mb-4 sm:hidden">
      Convierte tus metas en <span class="text-[#6255FA]">logros</span>
    </h1>
    <!-- Título desktop -->
    <h1 class="hidden sm:block text-5xl font-bold mb-5 leading-tight">
      Convierte tus <br />
      metas en <span class="text-[#6255FA]">logros</span>
    </h1>

    <!-- Banner descuento -->
    <div class="relative inline-block mb-6">
      <p class="px-4 py-2 rounded-full font-semibold text-sm bg-[#6255FA] shadow-md sm:hidden">
        🎉 Tu primera clase con 50% de descuento
      </p>
      <p class="hidden sm:inline-block px-5 py-2 rounded-full font-semibold text-base bg-[#6255FA] shadow-md">
        🎉 Tu primera clase con 50% de descuento
      </p>
    </div>

    <!-- Subtexto móvil -->
    <p class="mb-5 text-base sm:hidden font-medium text-white">
      Reserva una clase privada con un docente enfocado solo en ti.
    </p>
    <!-- Subtexto desktop -->
    <p class="hidden sm:block mb-8 text-lg font-medium text-white">
      Reserva una clase privada con un docente enfocado solo en ti.
    </p>

    <!-- Botón WhatsApp -->
    <a
      id="wspp-hero"
      href="https://wa.me/51996096191"
      rel="noopener"
      target="_blank"
      class="inline-flex items-center gap-2 text-white font-semibold text-base sm:text-lg px-6 py-3 rounded-xl bg-[#6255FA] hover:bg-[#4e44d8] transition duration-300"
    >
      <img src="/svg/whatsapp.svg" alt="WhatsApp" class="w-5 h-5" />
      Contactar un asesor
    </a>

    <!-- Más información -->
    <div class="mt-10 flex justify-center lg:justify-start">
      <a
        href="#beneficios"
        class="group inline-flex flex-col items-center text-white text-sm font-medium hover:opacity-90 transition"
      >
        Más información
        <svg
          class="mt-1 w-5 h-5 animate-bounce group-hover:translate-y-1 transition-transform"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
        </svg>
      </a>
    </div>
  </div>

  <!-- Ilustración Lottie escritorio -->
  <div class="hidden lg:flex justify-center items-center order-2 h-full px-6 relative">
    <div class="absolute w-[320px] h-[420px] bg-white/10 rounded-3xl blur-2xl z-0"></div>
    <script src="https://unpkg.com/@dotlottie/player-component@2.7.12/dist/dotlottie-player.mjs" type="module"></script>
    <dotlottie-player
      src="https://lottie.host/463208d5-6946-44c6-aa6f-d368fc70683d/guEAFpqxHs.lottie"
      background="transparent"
      speed="1"
      style="width: 100%; height: 100%"
      loop
      autoplay
      class="animate-fade-in"
    ></dotlottie-player>
  </div>
</main>



<!-- ESTILOS -->
<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }

  @keyframes wave-line {
    0% {
      transform: translateX(0) rotate(0deg) scaleY(1);
    }
    50% {
      transform: translateX(-20px) rotate(10deg) scaleY(1.05);
    }
    100% {
      transform: translateX(0) rotate(0deg) scaleY(1);
    }
  }
  .animate-wave-line {
    animation: wave-line 15s ease-in-out infinite;
    transform-origin: center;
  }

  .clip-triangle {
    clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
  }
  .clip-diamond {
    clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
  }

  @keyframes float1 {
    0%,
    100% {
      transform: translateY(0) scale(1);
    }
    50% {
      transform: translateY(-20px) scale(1.1);
    }
  }
  @keyframes float2 {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(30px);
    }
  }
  @keyframes float3 {
    0%,
    100% {
      transform: translateX(0) rotate(0deg);
    }
    50% {
      transform: translateX(25px) rotate(180deg);
    }
  }
  @keyframes float4 {
    0%,
    100% {
      transform: translate(-5px, 5px);
    }
    50% {
      transform: translate(10px, -10px);
    }
  }
  @keyframes float5 {
    0%,
    100% {
      transform: translateY(0) rotate(45deg);
    }
    50% {
      transform: translateY(-12px) rotate(90deg);
    }
  }
  @keyframes float6 {
    0%,
    100% {
      transform: translateX(0) scale(1);
    }
    50% {
      transform: translateX(-20px) scale(1.1);
    }
  }
  @keyframes float7 {
    0%,
    100% {
      transform: translate(-5px, 0);
    }
    50% {
      transform: translate(10px, 10px);
    }
  }
  @keyframes float8 {
    0%,
    100% {
      transform: rotate(0deg);
    }
    50% {
      transform: rotate(180deg);
    }
  }

  .animate-float1 {
    animation: float1 12s ease-in-out infinite;
  }
  .animate-float2 {
    animation: float2 14s ease-in-out infinite;
  }
  .animate-float3 {
    animation: float3 16s ease-in-out infinite;
  }
  .animate-float4 {
    animation: float4 18s ease-in-out infinite;
  }
  .animate-float5 {
    animation: float5 13s ease-in-out infinite;
  }
  .animate-float6 {
    animation: float6 14s ease-in-out infinite;
  }
  .animate-float7 {
    animation: float7 16s ease-in-out infinite;
  }
  .animate-float8 {
    animation: float8 18s linear infinite;
  }
</style>

<main
  id="inicio"
  class="scroll-mt-[64px] grid lg:grid-cols-2 place-items-center pt-16 pb-6 overflow-hidden relative bg-gradient-to-br from-[#B9A7FF] to-[#6255FA] min-h-screen"
>
  <!-- Decoración inferior derecha solo móvil -->
  <img
    src="/svg/deco_corner.svg"
    alt="Decoración inferior"
    class="absolute bottom-4 right-4 w-12 h-12 z-0 lg:hidden opacity-60"
  />

  <!-- Ilustración en responsive arriba del texto -->
  <div class="flex lg:hidden justify-center mb-6 px-6 order-1">
    <img
      src="/svg/illustracion.png"
      alt="Ilustración de metas y logros"
      class="object-contain w-[220px] sm:w-[280px] md:w-[320px] h-auto animate-fade-in"
      loading="eager"
    />
  </div>

  <!-- Contenido textual -->
  <div class="z-20 max-w-xl px-6 text-white order-2 lg:order-1 relative">
    <!-- Texto promocional -->
    <div class="relative inline-block mb-6">
      <p class="px-5 py-2 rounded-full font-semibold text-sm sm:text-base bg-[#6255FA] shadow-md">
        🎉 Tu primera clase con 50% de descuento
      </p>
    </div>

    <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-4 leading-tight">
      Convierte tus <br />
      metas en <span class="text-[#6255FA]">logros</span>
    </h1>

    <p class="mb-6 text-base sm:text-lg font-medium text-white">
      Reserva una clase privada con un docente enfocado solo en ti.
    </p>

    <!-- Botón WhatsApp -->
    <a
      id="wspp-hero"
      href="https://wa.me/51996096191"
      rel="noopener"
      target="_blank"
      class="inline-flex items-center gap-2 text-white font-semibold text-base sm:text-lg px-6 py-3 rounded-xl bg-[#6255FA] hover:bg-[#4e44d8] transition duration-300"
    >
      <img src="/svg/whatsapp.svg" alt="WhatsApp" class="w-5 h-5" />
      Contactar un asesor
    </a>

    <!-- Más información -->
    <div class="mt-10 flex justify-center lg:justify-start">
      <a
        href="#beneficios"
        class="group inline-flex flex-col items-center text-white text-sm font-medium hover:opacity-90 transition"
      >
        Más información
        <svg
          class="mt-1 w-5 h-5 animate-bounce group-hover:translate-y-1 transition-transform"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          viewBox="0 0 24 24"
        >
          <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
        </svg>
      </a>
    </div>
  </div>

  <!-- Ilustración en escritorio al costado -->
  <div class="hidden lg:flex justify-center items-center order-2 h-full px-6">
    <img
      src="/svg/illustracion_animada_bounce.svg"
      alt="Ilustración de metas y logros"
      class="object-contain w-[430px] max-h-[85vh] animate-fade-in"
      loading="eager"
    />
  </div>
</main>

<!-- Animación personalizada -->
<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }
</style>
